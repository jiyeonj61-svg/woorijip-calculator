# 우리집계산기 자동 계산 업데이트 적용 방법

이번 업데이트는 `site.config.json`을 건드리지 않습니다. 기존 도메인·구글/네이버 인증값·이메일 설정은 그대로 유지됩니다.

## GitHub에 적용할 파일

아래 5개 파일만 기존 파일과 교체합니다.

- `src/assets/calculators.js`
- `src/assets/site.js`
- `src/assets/styles.css`
- `tools/build.mjs`
- `tools/check.mjs`

## 가장 쉬운 적용 순서

1. `woorijip-auto-calc-update.zip`을 압축 해제합니다.
2. GitHub의 `woorijip-calculator` 저장소 첫 화면에서 **Add file → Upload files**를 누릅니다.
3. 압축 해제한 폴더 안의 `src`와 `tools` 폴더를 업로드 영역으로 끌어다 놓습니다.
4. 동일 경로의 기존 파일이 교체되는지 확인합니다.
5. 커밋 메시지에 `모든 계산기 입력 즉시 자동 계산 적용`을 입력하고 **Commit changes**를 누릅니다.
6. Cloudflare Pages → Deployments에서 최신 배포가 **Success**인지 확인합니다.
7. 3.3% 계산기 페이지를 새로고침한 뒤 금액을 바꿔 봅니다. 계산하기 버튼을 누르지 않아도 약 0.14초 뒤 결과가 자동 갱신됩니다.

## 바뀐 점

- 20개 계산기 모두 첫 화면에서 기본 예시값 결과 자동 표시
- 입력, 라디오 탭, 체크박스, 선택값 변경 시 자동 재계산
- 기존 `계산하기`, `예시 입력`, `초기화` 버튼 유지
- 3.3% 계산기와 부가세 계산기의 원 단위 처리 설정을 금액 입력 아래에 배치
- 평수 계산기의 59㎡·74㎡·84㎡·102㎡ 빠른 선택도 즉시 계산
- 초기화 후에도 기본값 결과를 자동 표시
- 금액 입력칸을 클릭하면 기존 금액이 전체 선택되어 바로 덮어쓸 수 있음

## 주의

`site.config.json`은 업로드하지 마세요. 현재 연결된 도메인과 검색엔진 인증값을 그대로 유지하기 위한 것입니다.
